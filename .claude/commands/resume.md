You're picking up an in-progress project in a fresh chat. Catch up and continue — no pasted prompt needed.

1. Read `CLAUDE.md`, `TODO.md`, `HANDOFF.md`, and `CSS-MAP.md` (in that order). Project memory auto-loads.
2. In 3-5 lines tell DJ: where the build stands, anything waiting on his decision, and the immediate NEXT task (from TODO.md's NEXT list).
3. Start the preview (Claude Preview MCP, name `monuments`, port 4321) and resize to 1280-1440px before judging any layout (the headless preview renders ~755px and hides desktop bugs).
4. Follow CLAUDE.md: solo repo (no sync-check each turn), auto-push at the end of each change, verify at desktop width, one-column layout system (`--wrap`/`--pad`), no em dashes.
5. Begin the NEXT task unless DJ redirects.
