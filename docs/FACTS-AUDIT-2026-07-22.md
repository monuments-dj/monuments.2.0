# FACTS AUDIT · 2026-07-22 · the Phase-1 extraction map

Every shared fact that is currently hardcoded in markup, and where it lives.
This is the checklist for Phase 1 (extract into `src/data/*` so monuments +
djthecd edit in ONE place). Generated by sweep; counts verified.

## 1. Email `monuments.cc@gmail.com` → `src/data/site.js`
- **33 files** carry it (every page's mailto + mobile menus + CTAs).
  Heaviest: contact (5), index (3), hire (3), ai (2), every work page (1).
- Extraction: `site.email`, consumed via component props / imports.

## 2. Locations / identity → `src/data/site.js`
- "LA / Boise" family strings: **9 files** (hero NFOs, corners, footers).
- `Est. MMXXII`: 1 file (about). Cities, est-year, wordmark refs = site.js.

## 3. Claims / stats → `src/data/claims.js` (with `verified:` flags)
- AchievementsTicker (renders on about + work): Emmy · 4x revenue ·
  **3.6M+ (STILL UNVERIFIED)** · 5→30+ · 2M+ · roster line.
- StatRow props are inline per work page (19 pages) - Phase 2 scope.
- giving statrow: page-local markup (4 stats).
- Rule: a claim gets ONE row here with a `verified: true/false` field;
  unverified claims render nowhere (kills the 160M+/3.6M+ class forever).

## 4. Brand roster → `src/data/brands.js`
- Homepage Trusted board: lives TWICE in index.astro (seeded cells markup
  + the TB script array, 13 brands). Both should generate from one export.
- Logo variants exist per brand (black/white/color) in /public/logos.

## 5. Testimonials → `src/data/testimonials.js`
- 10-person roster + quotes lives INSIDE Testimonials.astro frontmatter.
  Jake Goble's is the only landed quote (verbatim, linked).

## 6. Team → `src/data/team.js` (NEW, pending DJ)
- Kova: DP + Editor (name spelling unconfirmed - CWI credits say
  "Kova Kovatonic"). Tessanie: name + role OWED. DJ: title line TBC.

## 7. Credits (Call Sheets) → Phase 2
- Per-work-page CallSheet props, inline. Migrates with case studies.
- CREDITS-AUDIT.md is the truth reference.

## Known rot fixed alongside this audit
- Duplicate `<meta name="description">` on 21 pages (og:begin block +
  page's own). Fixed 2026-07-22: page's richer description is the single
  source; og:description synced to it.

## Phase-1 wrinkle (recorded so the diff harness expects it)
- index.astro contact sheet RANDOMIZES per load (`POOL` shuffle) - the
  zero-diff proof must exclude `#frames` or seed the shuffle.
